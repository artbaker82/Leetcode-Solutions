/* Create a function in Javascript that takes two integers and returns a string containing
 the quotient and the remainder using only plus and minus as arithmetic operators.
 */

const getQuotient = (x, y) => {
  solution = "Hello";
  return solution;
};

console.log(getQuotient(36, 3));
